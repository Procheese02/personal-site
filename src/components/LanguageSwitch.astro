---
interface Props {
  href: string;
  label: string;
}
const { href, label } = Astro.props;
---

<a href={href} class="rounded-full border border-line bg-white/5 px-4 py-2 text-sm font-medium text-slate-200 transition hover:border-cyan/60 hover:bg-cyan/10 hover:text-white">
  {label}
</a>
