---
import type { Project } from '../content/projects';
import type { Locale } from '../content/i18n';
import { copy } from '../content/i18n';

interface Props {
  project: Project;
  locale: Locale;
}

const { project, locale } = Astro.props;
const t = copy[locale];
const summary = locale === 'zh' ? project.summaryZh : project.summary;
const category = locale === 'zh' ? project.categoryZh : project.category;
const highlights = locale === 'zh' ? project.highlightsZh : project.highlights;
---

<article class:list={['group rounded-[1.75rem] border bg-panel/70 p-6 shadow-xl shadow-black/20 backdrop-blur transition hover:-translate-y-1 hover:border-cyan/50', project.featured ? 'border-cyan/35 lg:col-span-2' : 'border-line']}>
  <div class="mb-5 flex flex-wrap items-start justify-between gap-4">
    <div>
      {project.featured && <p class="mb-2 text-xs font-semibold uppercase tracking-[0.24em] text-cyan">{t.featured}</p>}
      <h3 class="text-2xl font-semibold text-white">{project.name}</h3>
      <p class="mt-2 text-sm text-slate-400">{category} · {project.period}</p>
    </div>
  </div>
  <p class="text-base leading-7 text-slate-300">{summary}</p>
  <ul class="mt-5 space-y-3 text-sm leading-6 text-slate-300">
    {highlights.map((item) => <li class="flex gap-3"><span class="mt-2 h-1.5 w-1.5 flex-none rounded-full bg-cyan"></span><span>{item}</span></li>)}
  </ul>
  <div class="mt-6 flex flex-wrap items-center gap-3">
    {project.tech.map((tech) => <span class="rounded-full border border-line bg-white/[0.04] px-3 py-1 text-xs text-slate-300">{tech}</span>)}
  </div>
  {project.githubUrl && (
    <a
      href={project.githubUrl}
      target="_blank"
      rel="noreferrer"
      class="github-action mt-6 inline-flex items-center gap-2 rounded-full border border-line bg-white/5 px-4 py-2 text-sm font-semibold text-slate-200 transition hover:border-cyan/60 hover:bg-cyan/10 hover:text-white"
    >
      <svg class="h-4 w-4" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path fill-rule="evenodd" d="M12 2C6.48 2 2 6.58 2 12.23c0 4.52 2.87 8.35 6.84 9.71.5.1.68-.22.68-.49 0-.24-.01-1.04-.01-1.88-2.78.62-3.37-1.22-3.37-1.22-.45-1.18-1.11-1.5-1.11-1.5-.91-.64.07-.63.07-.63 1 .07 1.53 1.06 1.53 1.06.9 1.57 2.36 1.12 2.93.86.09-.67.35-1.12.64-1.38-2.22-.26-4.56-1.14-4.56-5.06 0-1.12.39-2.03 1.03-2.75-.1-.26-.45-1.3.1-2.71 0 0 .84-.28 2.75 1.05A9.34 9.34 0 0 1 12 6.95c.85 0 1.7.12 2.5.34 1.9-1.33 2.74-1.05 2.74-1.05.55 1.41.2 2.45.1 2.71.64.72 1.03 1.63 1.03 2.75 0 3.93-2.34 4.8-4.57 5.05.36.32.68.94.68 1.9 0 1.37-.01 2.47-.01 2.8 0 .27.18.59.69.49A10.14 10.14 0 0 0 22 12.23C22 6.58 17.52 2 12 2Z" clip-rule="evenodd" />
      </svg>
      <span>{t.viewOnGithub}</span>
    </a>
  )}
</article>
